pipeline {
  agent any
  stages {
    stage('Clean Workspace'){
      steps {
        cleanWs()
      }
    }
    stage ('Checkout') {
      steps {
        checkout scm
      }
    }          
    stage('Testing...') {
    agent{
        docker{image 'postman/newman'}
      }
      steps{
        sh 'docker run -t postman/newman run postman/test.postman_collection.jso'
      }
    }
  }
}
