pipeline {
  agent any
  tools {nodejs "latest"}
  stages {    
    stage ('Checkout') {
      steps {
          checkout scm
      }
    }          
    stage('Test') {
      steps {
          sh 'npm config ls'
      }
    }             
  }
}
